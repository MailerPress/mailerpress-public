import {useMemo, useState} from "react";
import {DataViews} from "@wordpress/dataviews";

export function MyCustomPageTable() {
    const onChangeView = () => { /* React to user changes. */ }

    return (
        <DataViews/>
    );
}